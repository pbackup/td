### Project properties ###

# Basic
project.name=project-template
project.version=1.0
project.password=project-template

# Targets
#project.target.build=local
#project.target.deploy=dev

# Files
#project.files.upload=
#project.files.ignore=
#project.files.chmod=


### Build properties ###

# LOCAL
local.type=local
local.path.home=${basedir}
local.path.build=${local.path.home}/build
local.path.dist=${local.path.home}/dist
local.path.src=${local.path.home}/src
local.shell.host=
local.shell.username=
local.shell.password=
local.database.host=localhost
local.database.name=${project.name}
local.database.username=${project.name}
local.database.password=${project.name}
local.mail.host=${project.name}.dhaka.codemate.com
local.mail.username=${project.name}
local.mail.password=${project.password}

# DEV
dev.type=remote
dev.path.home=/home/${project.name}/domains/dev.${project.name}.dhaka.codemate.com
dev.path.build=${dev.path.home}/${project.name}-packs
dev.path.dist=${dev.path.home}/${project.name}-packs
dev.path.src=${dev.path.home}/${project.name}-${project.version}-dev
dev.shell.host=dev.${project.name}.dhaka.codemate.com
dev.shell.username=${project.name}
dev.shell.password=${project.password}
dev.database.host=localhost
dev.database.name=dev_${project.name}
dev.database.username=${project.name}
dev.database.password=${project.password}
dev.mail.host=${project.name}.dhaka.codemate.com
dev.mail.username=${project.name}
dev.mail.password=${project.password}

# TEST
test.type=remote
test.path.home=/home/${project.name}/domains/test.${project.name}.dhaka.codemate.com
test.path.build=${test.path.home}/${project.name}-packs
test.path.dist=${test.path.home}/${project.name}-packs
test.path.src=${test.path.home}/${project.name}-${project.version}-test
test.shell.host=test.${project.name}.dhaka.codemate.com
test.shell.username=${project.name}
test.shell.password=${project.password}
test.database.host=localhost
test.database.name=test_${project.name}
test.database.username=${project.name}
test.database.password=${project.password}
test.mail.host=${project.name}.dhaka.codemate.com
test.mail.username=${project.name}
test.mail.password=${project.password}

# PROD
prod.type=remote
prod.path.home=/home/${project.name}/${project.name}
prod.path.build=${prod.path.home}/${project.name}-packs
prod.path.dist=${prod.path.home}/${project.name}-packs
prod.path.src=${prod.path.home}/${project.name}-${project.version}
prod.shell.host=${project.name}.com
prod.shell.username=${project.name}
prod.shell.password=${project.password}
prod.database.host=localhost
prod.database.name=${project.name}
prod.database.username=${project.name}
prod.database.password=${project.password}
prod.mail.host=${project.name}.com
prod.mail.username=${project.name}
prod.mail.password=${project.password}
