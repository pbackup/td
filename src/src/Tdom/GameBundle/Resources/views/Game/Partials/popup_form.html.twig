<div class="modal" id="add_game">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title">{{ "Add new game" | trans}}</h4>
            </div>
            <div class="modal-body" id="popup_form">
                {% include 'TdomGameBundle:Game:Partials/form.html.twig' %}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">{{ "Close" | trans }}</button>
                <button type="button" class="btn btn-danger" id="add_new_game">{{ "Add" | trans }}</button>
                <button type="button" class="btn btn-danger" id="add_game_n_close"> {{ "Add and close" | trans }}</button>
            </div>
        </div>
    </div>
</div>